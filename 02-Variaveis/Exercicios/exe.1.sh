#O que é impresso na tela após a execução do seguinte script?
# Explique a execução de cada linha do script.

#!/bin/bash

a=10  		# Atribui o valor 10 a variavel "a"
b=20		# Atribui o valor 20 a variavel "b"
c=$a$b		# Atribui os valores das variaveis "a" e "b"
		# na nova varial "c" 
d=$a$b$c	# Atribui os valores das "a", "b","c"
		# na varial "d"
echo ${d} 	# Exibe o valor atribuido da variavel "d"


# Explique a execução de cada linha do script.
